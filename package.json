{
  "name": "crypto-trader",
  "version": "0.1.0",
  "description": "A terminal-based crypto trading assistant with configurable risk thresholds and paper trading mode.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types \"tests/*.test.ts\"",
    "verify": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "ccxt": "^4.0.0",
    "blessed": "^0.1.81",
    "blessed-contrib": "^4.11.0"
  },
  "license": "MIT"
}